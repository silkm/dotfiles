#!/bin/sh
# Writes ~/.doom.d/secrets.el from 1Password.
# Re-runs automatically whenever the 1Password blob changes.
cat > "$HOME/.doom.d/secrets.el" << 'EOF'
{{ onepasswordRead "op://Private/chezmoi_doom_emacs_secrets.el/notesPlain" }}
EOF
